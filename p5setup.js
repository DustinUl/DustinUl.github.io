function setup() {
  createCanvas(700, 700);
  frameRate(30);
}

window.addEventListener("resize", function () {
  resizeCanvas(700, 700);
  clear();
});

new p5();
var width = 700;
var height = 700;
