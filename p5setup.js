function setup() {
  createCanvas(700, 600);
  frameRate(45);
}

window.addEventListener("resize", function () {
  resizeCanvas(700, 600);
  clear();
});

new p5();
var width = 700;
var height = 600;
